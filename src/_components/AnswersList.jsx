const AnswersList = ({ answers }) => {

    console.log("HELLO");

    return (
        <div>
            {/* {answers.map((answer, i) => (
                <div>
                    {answers[i]}
                </div>
            ))} */}
        </div>
    );
};

export default AnswersList;
